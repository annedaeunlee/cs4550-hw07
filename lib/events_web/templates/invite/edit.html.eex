<h1>Edit Invite</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.invite_path(@conn, :update, @invite)) %>

<span><%= link "Back", to: Routes.post_path(@conn, :show, @invite.post_id) %></span>
